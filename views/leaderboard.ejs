<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>Top 10</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
<%- include('partials/header', { user: user }) %>
<div class="centered main-bg">
  <h2>Top 10 – XP</h2>
  <table class="leaderboard-table">
    <tr><th>Platz</th><th>Name</th><th>XP</th><th>Level</th></tr>
    <% (users || []).forEach((u,i) => { %>
      <tr>
        <td><%= i+1 %></td><td><%= u.username %></td><td><%= u.xp %></td><td><%= u.level %></td>
      </tr>
    <% }); %>
  </table>
</div>
<%- include('partials/footer') %>
</body>
</html>